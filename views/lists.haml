
%h1
  ALL LISTS

%hr

- haml :flash_lists, :layout => :flash_layout

- re_order(@lists).each do |list|
  %p
    %a(href="/lists/#{list.id}" class="box-link#{ ' done' if list.done? }")
      #{list.name} (#{list.count_done_items}/#{list.count_all_items})

.major
  %p
    %a(href="/lists/new")
      + NEW LIST

%hr
